/**
 * Set to `true` to load input data from 'example.txt' instead of 'input.txt'
 */
export const USE_EXAMPLE = <boolean> false

/**
 * Set to `true` to output additional information for debugging purposes, if applicable.
 */
export const DEBUG = <boolean> false
